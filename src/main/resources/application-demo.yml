spring:
  datasource:
    url: jdbc:h2:mem:recipekrdemo;MODE=MySQL;DATABASE_TO_LOWER=TRUE;NON_KEYWORDS=USER
    username: sa
    password:
    driver-class-name: org.h2.Driver

  sql:
    init:
      mode: always
      schema-locations: classpath:sql/demo_schema.sql
      data-locations: classpath:sql/demo_data.sql

demo:
  mode: true
